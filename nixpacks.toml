cacheDirectories = []

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production npm start"
