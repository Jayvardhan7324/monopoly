cacheDirectories = []

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["rm -f package-lock.json && npm install --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production npm start"
